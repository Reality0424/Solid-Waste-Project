# Phase 1 最小依赖（知识图谱 + TransE）
# 仅阶段1所需，远比根目录 requirements.txt 轻量（无需 torch/tensorflow/dgl）。
# 安装: python3 -m venv .venv && .venv/bin/pip install -r phase1/requirements-phase1.txt

pandas>=2.0
numpy>=1.24
neo4j>=5.0
scikit-learn>=1.3
matplotlib>=3.7
PyYAML>=6.0
networkx>=3.0
